<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.taufiq.bwamov">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.noActionBar">
        <activity android:name=".DetailMovieActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".boarding.OnBoardingOneActivity" />
        <activity android:name=".boarding.OnBoardingTwoActivity" />
        <activity android:name=".boarding.OnBoardingThreeActivity" />
        <activity android:name=".login.signin.SignInActivity" />
        <activity android:name=".login.signup.SignUpActivity" />
        <activity
            android:name=".login.signup.SignUpPhotoActivity"/>
        <activity android:name=".PilihBangkuActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".boarding.SuccessBeliActivity" />
        <activity android:name=".MyWalletActivity" />

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>

    </application>

</manifest>